# Sensitive / credentials
auth.json
agents/.env.browser
.env
extensions/.env

# Session data (large)
sessions/

# Local data
.data/

# Generated / logs
daily-analysis/
daily-retrospective*.log
pi-crash.log
.last-pi-retro-*
.last-morning-briefing-*
.last-weekly-retro-*
.last-weekly-rollup-*
morning-briefing*.log
weekly-retrospective*.log
weekly-rollup*.log
retrospective/

# Coverage outputs / reports
coverage/
*coverage*.log
*coverage*-summary*.json
coverage-final.json
lcov.info
*.lcov

# Scripts (local automation)
bin/
local-scripts/

# Daily retro SVG
tmp/daily-retro.svg

# Internal pi data
.pi/
.pi-lens/
state/
.context/
bookmarks.json

# OS / IDE
.DS_Store

# Dependencies
node_modules/

# Extension-specific ignores
extensions/.context/
extensions/.gw/
extensions/pi-session-*.html

# until presets (personal)
extensions/until-presets/

# usage reporter (personal)
extensions/usage-reporter/

# claude-mcp-bridge local runtime settings
claude-mcp-bridge-tools.json
claude-mcp-bridge-cache.json
extensions/remote/dist/
extensions/remote/web-dist/
